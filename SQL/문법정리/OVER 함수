OVER함수는 각 행별로 특정 기준에 따라 필요한 집합을 구해 함수를 적용하고 싶을 때 쓰는 구문이다.

• 전통 SQL 사용 예시
SELECT YYMM, PRICE
FROM (
SELECT YYMM,
SUM(TOT_PRICE) AS PRICE
FROM TABLE1
GROUP BY YYMM
ORDER BY YYMM DESC
)

• OVER 함수 이용
SELECT YYMM,
SUM(TOT_PRICE) OVER(ORDER BY YYMM DESC) AS PRICE
FROM TABLE1
