cross join은 두 테이블의 모든 가능한 행 조합을 생성하는 반면
natural join은 두 테이블에서 이름이 같은 모든 컬럼을 기준으로 자동으로 조인 조건을 설정하고
일치하는 행만 결과로 반환합니다.
cross join은 조인 조건이 없으며 모든 조합을 만들지만
natural join은 이름이 같은 컬럼을 조건으로 사용하므로 조인 조건이 자동으로 설정됩니다.
